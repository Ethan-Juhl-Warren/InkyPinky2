# InkyPinky2

## Dependencies
    - Odin

## To build engine

First install Odin
Arch:
``` bash
yay -S odin
```

Build engine:

``` bash
make linux-engine          # Linux
```

``` bash
.\run.ps1 -Engine          # Windows
```

Run the editor:
``` bash
cd src/editor/
npm install
npm start
```
